{
  "name": "eg16",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build-a": "npm run assets && npm run build",
    "build-ap": "npm run build-a && npm run preview",
    "assets": "npm run bitmap && npm run font",
    "bitmap": "node scripts/convertBitmap",
    "font": "node scripts/convertFont"
  },
  "devDependencies": {
    "@types/node": "^20.5.9",
    "typescript": "^5.0.2",
    "unplugin-fonts": "^1.0.3",
    "vite": "^4.4.5",
    "vite-tsconfig-paths": "^4.2.0"
  },
  "dependencies": {
    "canvas": "^2.11.2",
    "detect-collisions": "^9.1.1",
    "keycode": "^2.2.1",
    "pixi.js": "^7.2.4",
    "ts-key-enum": "^2.0.12"
  }
}
